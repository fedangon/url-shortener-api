/**
 * Camada responsável por expor os endpoints HTTP da API.
 * Recebe as requisições, delega o fluxo para os serviços e devolve as respostas ao cliente.
 */
package com.fedangon.urlshortener.controller;
